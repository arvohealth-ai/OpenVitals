[project]
name = "openvitals-sdk"
version = "0.1.0"
description = "Python SDK for OpenVitals demo APIs"
requires-python = ">=3.11"
dependencies = ["httpx>=0.28.0"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
